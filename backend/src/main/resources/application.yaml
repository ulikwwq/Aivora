spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/aivora
    username: aivora_user
    password: password123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

jwt:
  secret: aivora-super-secret-key-must-be-at-least-32-characters-long
  expiration: 86400000

groq:
  api-key: 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
  model: llama-3.3-70b-versatile

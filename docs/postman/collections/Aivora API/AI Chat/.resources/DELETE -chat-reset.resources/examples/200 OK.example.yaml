$kind: http-example
request:
  url: "{{baseUrl}}/chat/reset"
  method: DELETE
  headers:
    Authorization: Bearer {{token}}
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: |-
      {
        "message": "Session reset"
      }
order: 1000
